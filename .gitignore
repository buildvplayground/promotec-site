# Material bruto e pesado — não versionar
_raw/
*.psd
*.ai

# Segredos — NUNCA versionar (só o .example)
db-config.php
.env
.env.*
!.env.example
*-secret*
*credenciais*

# Build/infra
node_modules/
.vercel/
.DS_Store
*.log

# Uploads reais do servidor (versiona só o .htaccess de proteção)
uploads/*
!uploads/.htaccess
Site/uploads/*
!Site/uploads/.htaccess

# Material-fonte — fica local + Drive, não no repo (padrão BuildV).
# Site/ é versionado (é o entregável; hospedagem ainda indefinida — para
# Hostinger/estático, Site/ É a própria pasta de deploy).
imagens/
Marca/
Copys/
design-system/
brief-pack.md

# Claude Code local settings
.claude/settings.local.json

# Scaffolding dos geradores de página (Python) — não é o site em si
_build_tmp/

# Estado local de tooling
.claude/
